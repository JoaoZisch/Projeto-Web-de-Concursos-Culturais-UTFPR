<div class="container py-4">
    <div class="container">
        <main>

            <div class="row g-5">

                <div class="col-md-12 col-lg-12">

                    <form action="/login" method="post">
                        <div class="row g-3">
                            <center>

                                <div class="col-sm-6">
                                    <label for="model" class="form-label">Login</label>
                                    <input type="text" name="login" class="form-control">
                                </div>

                                <div class="col-sm-6">
                                    <label for="model" class="form-label">Senha</label>
                                    <input type="password" name="senha" class="form-control">
                                </div>
                                <br>
                                <button class="w-50 btn btn-lg btn-dark" type="submit">logar</button>

                            </center>
                        </div>
                    </form>
                    <br>
                    <script> function recuperarSenha(){ window.location.href = "./usuario/recuperarSenha" + document.getElementById("login").value }</script>
                    <button onclick=recuperarSenha()> Recupear Senha </button>
                </div>

            </div>
        </main>
    </div>
</div>